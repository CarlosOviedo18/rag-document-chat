anthropic          # cliente oficial de Claude (genera las respuestas)
voyageai           # embeddings multilingues (convierte texto -> vectores)
chromadb           # base de datos vectorial local
fastapi            # framework del backend web
uvicorn[standard]  # servidor que ejecuta FastAPI
python-dotenv      # lee las claves desde el archivo .env
